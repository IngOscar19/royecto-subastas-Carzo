# Network

- `ApiClient`: cliente dio con Base URL configurable por entorno y interceptor de JWT (token en flutter_secure_storage).
- `SocketClient`: cliente socket_io_client para subastas en tiempo real.

El contrato de eventos WebSocket y de API está definido en el AGENTS.md del repo. Pendiente de implementar.
