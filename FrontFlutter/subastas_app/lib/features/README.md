# Features

Features de la app organizadas por dominio: `auth`, `auctions` y `bids`. Cada feature se estructura en `data/` (repositorios y DTOs), `domain/` (modelos freezed) y `presentation/` (pantallas y providers de Riverpod).
